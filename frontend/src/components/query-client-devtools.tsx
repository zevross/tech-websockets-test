import { ReactQueryDevtools } from "@tanstack/react-query-devtools";

const QueryClientDevtools = () => (
  <ReactQueryDevtools buttonPosition="bottom-right" />
);

export default QueryClientDevtools;
